using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptsSample : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //수정사항 있음
        int i = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
